services:
  photomatch-web:
    image: nginx:alpine
    container_name: photomatch-web
    restart: unless-stopped
    ports:
      - "8092:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
